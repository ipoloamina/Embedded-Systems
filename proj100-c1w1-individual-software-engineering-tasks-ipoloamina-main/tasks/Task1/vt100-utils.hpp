#pragma once
#include "uop_msb.h"
using namespace uop_msb;


void clearTerminal();   //Clear the terminal
void homeCursor();      //Move the cursor position to the top left
void hideCursor();      //Hide the cursor (prevents flickering)
